export default function HomePage() {
  return (
    <main className="page">
      <section className="card">
        <h1>PersonaBot</h1>
        <p>Frontend baseline is ready.</p>
        <p>Next step: wire this UI to FastAPI WebSocket chat streaming.</p>
      </section>
    </main>
  );
}
