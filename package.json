{
  "name": "personabot",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm exec --workspace apps/web next dev",
    "build:web": "npm exec --workspace apps/web next build",
    "lint:web": "npm exec --workspace apps/web next lint"
  }
}
